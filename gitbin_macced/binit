#!/bin/bash

function whatis() {
	cuseco=$(basename $0)
 echo -e "\033[38;5;118m${cuseco}	⇒ 	\\033[38;5;11mtype:\\033[38;5;118mbin work ⇒  mv → /bin"
echo -e " \033[38;5;195m sends cprue/sh files to BIN without .sh \\033[38;5;212m ❤️️ \\n\\033[38;5;43m\t👉\t\033[38;5;11m${cuseco} -$o\033[0m\\n
    "
}

#VARIABLES
jit=false

##DIRECTORIES
bindir=/Users/violetagonzalezgarcia/gitclones/bin
jitdir=/Users/violetagonzalezgarcia/gitclones/bin/jithelpers
##FILES
##FUNCTIONS
source Opte.fu
source Asksure.fu
source GralGetops.fu
#gralgetops

### MAIN
while getopts ":ehj" o; do
	case "${o}" in
		e)
			opte
			exit
			;;
		h)
			whatis 2>/dev/null
			[[ "$?" == 25  ]] || echo -e "PENDING whatis!! 🔥 "
			exit
			;;
		j)
			jit=true
			;;
		\?)
			echo "invalid option: -$OPTARG"
			exit
			;;
	esac
done
shift $((OPTIND-1))
# ♥

dshfile=$1
dfile=$(realpath ${dshfile}) 

if [[ ${jit} = true ]]; then
	outdir=${jitdir}
else
	outdir=${bindir}     
fi

mv ${dfile} ${outdir}/${dshfile%.sh} 

ls ${outdir}  
